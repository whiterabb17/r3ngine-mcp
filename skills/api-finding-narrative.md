# API finding narrative

For API/auth/schema issues from MCP:
- Name the endpoint and method if present
- State whether auth was required / missing from evidence only
- Do not invent auth bypass steps
- Recommend verification (re-scan singular tool / workflow) via propose_followups, not ad-hoc exploits
