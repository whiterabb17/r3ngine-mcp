# TLS noise grouping

Group cipher/protocol findings by host:
- One bullet per host with count of related findings
- Keep one representative vuln id in the appendix
- Do not recommend “disable TLS” without naming the weak protocol from evidence
