# Evidence citation

Every claim: `scan_id` + asset (subdomain/endpoint id or URL) + tool/result id when available.
Page with limit/offset; never invent remaining rows.
If MCP errors: show error, retry once, stop if it persists.
