# Note hygiene

After plan done/abort/retry:
- Update or create a TodoNote with plan_id, step outcomes, and evidence ids (activity_id, workflow_id, vuln ids)
- Mark abort/retry attempts explicitly
- Never delete notes via MCP
